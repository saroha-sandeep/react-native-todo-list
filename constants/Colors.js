import { StyleSheet } from 'react-native'

export const GlobalColors = StyleSheet.create({
    primaryText: {
        color: '#ddd'
    },
    primaryBackground: {
        backgroundColor: '#ddd'
    }
}) 